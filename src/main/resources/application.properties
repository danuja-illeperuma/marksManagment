#spring.application.name=marksManagement


## 3. Database Configuration (MySQL Example)
#spring.datasource.url=jdbc:mysql://localhost:3306/marksdb
#spring.datasource.username=root
#spring.datasource.password=1234
#
#
## 4. JPA / Hibernate Settings
#spring.jpa.hibernate.ddl-auto=validate
#
#spring.jpa.show-sql=true

## 3. Database Configuration for Railway
spring.application.name=marksManagement

spring.datasource.url=jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQLDATABASE}?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=${MYSQLUSER}
spring.datasource.password=${MYSQLPASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

server.port=${PORT:8080}